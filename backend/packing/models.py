"""
Packing app deprecated: packing models were consolidated into
`trips.models.PackingItem` per project architecture.

This module is intentionally left empty to avoid duplicate model
definitions while preserving the app folder for any legacy code.
"""

# No models in this module. See `trips.models` for packing-related models.
